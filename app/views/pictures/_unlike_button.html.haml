= button_to like_picture_path(picture), method: :delete, class: "btn btn-default", remote: true do
		%span.glyphicon.glyphicon-heart.liked
		%span{id: "likes-count-#{picture.id}"}= picture.get_likes.size