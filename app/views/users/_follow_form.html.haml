-# local variable 'user' needs to be provided
- unless current_user.id == user.id
	%div{id: "follow_form_#{user.id}"}
		- if current_user.following? user
			= render 'unfollow', user: user
		- else
			= render 'follow', user: user