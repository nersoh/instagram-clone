= button_to(current_user.active_relationships.build, form_class: 'inline-button', class: 'btn btn-success btn-xs', remote: true) do
					= hidden_field_tag :followed_id, user.id
					%span.glyphicon.glyphicon-plus
					Follow